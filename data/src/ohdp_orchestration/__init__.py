"""Dagster code location for the Open Health Data Platform.

The package is named ``ohdp_orchestration`` rather than ``dagster`` so it does
not shadow the ``dagster`` library on sys.path (ADR-0004).
"""
