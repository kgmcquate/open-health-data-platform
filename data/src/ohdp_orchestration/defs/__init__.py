"""Autoloaded defs tree (Dagster components).

``ohdp_orchestration.definitions`` calls ``load_defs`` on this package, so every
component instance discovered under here is merged into the single code-location
``Definitions`` object.
"""
