#!/usr/bin/env bash
# Bring up k3s on a fresh VM and apply the base manifests.
# Stub — see ARCHITECTURE.md §4, M1.
set -euo pipefail
echo "TODO(M1): install k3s, apply platform/k3s/base, install cert-manager"
