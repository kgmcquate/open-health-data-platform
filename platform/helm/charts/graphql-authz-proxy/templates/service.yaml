apiVersion: v1
kind: Service
metadata:
  name: {{ include "gqlproxy.fullname" . }}
  labels:
    {{- include "gqlproxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gqlproxy.selectorLabels" . | nindent 4 }}
