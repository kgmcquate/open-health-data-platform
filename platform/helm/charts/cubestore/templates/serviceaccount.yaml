{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cubestore.serviceAccountName" . }}
  labels:
    {{- include "cubestore.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
# cubestore never calls the Kubernetes API. No RBAC is granted on purpose.
automountServiceAccountToken: false
{{- end }}
